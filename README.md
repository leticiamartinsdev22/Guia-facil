# Guia Fácil — MVP

Esqueleto da Fase 1: chat web com um agente de IA que ensina tecnologia passo a
passo.

## Como rodar
```bash
npm install
cp .env.example .env.local   # cole sua chave da Anthropic em .env.local
npm run dev
